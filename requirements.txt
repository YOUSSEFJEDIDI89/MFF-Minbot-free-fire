# VortexVPN Python dependencies
# --------------------------------
# Pinned to minor versions for reproducibility.
cryptography>=42,<46        # AES-GCM, X25519, Argon2id, etc.
flask>=3.0,<4.0             # Web panel
werkzeug>=3.0,<4.0          # Flask dependency (pinned to keep security fixes)
argon2-cffi>=23.1           # Argon2id password hashing (alt path)
pyyaml>=6.0                 # Optional YAML config (in addition to TOML)

# Test / dev
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Production WSGI server
gunicorn>=22.0
